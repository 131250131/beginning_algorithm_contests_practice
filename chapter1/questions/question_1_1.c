#include<stdio.h>

// 问题1:int型整数的最小值和最大值是多少(需要精确值)?

// 实验思路：从0开始++，什么时候符号转变，说明到了最大值

// 实验结果：	

// 2147483647 (1 << 32 -1)
// -2147483648 

int main() {
	int a = 0;
	while (a >= 0) {
		a++;
	}
	printf("%d\n", a - 1);
	printf("%d\n", a);
}